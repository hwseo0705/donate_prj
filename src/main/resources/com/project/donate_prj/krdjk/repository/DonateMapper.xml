<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.donate_prj.krdjk.repository.DonateMapper">
    
    <resultMap id="donateBoardMap" type="com.project.donate_prj.krdjk.domain.DonateBoard">
        <result property="boardNo" column="board_no" />
        <result property="targetMoney" column="target_money" />
        <result property="currentMoney" column="current_money" />
        <result property="startDate" column="start_date" />
        <result property="endDate" column="end_date" />
        <result property="likeCnt" column="like_cnt" />
    </resultMap>


    <select id="findAll" resultMap="donateBoardMap">
        SELECT *
        FROM donate_board
        ORDER BY board_no DESC
    </select>


    <select id="findOne" resultMap="donateBoardMap">
        SELECT *
        FROM donate_board
        WHERE board_no = #{boardNo}
    </select>


    <select id="getTotalCnt" resultType="int">
        SELECT COUNT(*)
        FROM donate_board
    </select>


    <update id="upLikeCnt">
        UPDATE donate_board
        SET like_cnt = like_cnt + 1
        WHERE board_no = #{boardNo}
    </update>


</mapper>
